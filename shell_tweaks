# zoxide
eval "$(zoxide init --cmd cd zsh)"

# crow translate
alias cr="crow -b -e google -t ru"

# fzf for shell completion
eval "$(fzf --zsh)"

# cat to bat
alias cat="bat"
export BAT_THEME=Nord

# fuck
eval $(thefuck --alias)
